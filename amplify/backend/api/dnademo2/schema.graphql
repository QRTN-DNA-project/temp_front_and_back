type TEST @model @auth(rules: [{allow: public}]) {
  id: ID!
  time: String!
  count: Int!
  class: String!
}

type MEMBER @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  address: String!
  sex: String!
  age: String!
  feature: String
  number: String
} 